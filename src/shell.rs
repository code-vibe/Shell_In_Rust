pub fn handle_command(command: &str) -> String {
    format!("{}: command not found", command.trim())
}


pub fn handle_input(input: &str) -> String {
    format!("{}: command not found", input.trim())
}

